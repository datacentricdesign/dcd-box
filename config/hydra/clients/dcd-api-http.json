{
  "client_id": "${API_HTTP__CLIENT_ID}",
  "name": "DCD - APP HTTP",
  "description": "The HTTP API of the Data-Centric Design hub.",
  "client_secret": "${API_HTTP_CLIENT_SECRET}",
  "scope": "",
  "grant_types": ["client_credentials", "refresh_token"],
  "response_types": ["token", "refresh_token"]
}
