{
  "client_id": "${APP_SUBJECT_CLIENT_ID}",
  "client_name": "DCD - Data Subject",
  "description": "The HTTP API of the Data-Centric Design hub.",
  "client_secret": "${APP_SUBJECT_CLIENT_SECRET}",
  "scope": "$APP_SUBJECT_SCOPE",
  "logo_uri": "${PROTOCOL}://${HOST}:${PORT}/auth/img/logo/android-icon-192x192.png",
  "grant_types": ["client_credentials", "refresh_token", "authorization_code"],
  "response_types": ["token", "refresh_token", "code"],
  "redirect_uris": [
    "${PROTOCOL}://${HOST}:${PORT}/subject/auth/callback",
    "http://localhost:8080/subject/auth/callback"
  ],
  "token_endpoint_auth_method": "client_secret_post"
}
